name: PR Checklist Validator

on:
  pull_request:
    branches:
      - master

jobs:
  verify-template:
    name: Verify PR Checkboxes
    runs-on: ubuntu-latest
    steps:
      - name: Validate PR description
        uses: actions/github-script@v7
        with:
          script: |
            const body = context.payload.pull_request?.body || "";
            
            const requiredChecks = [
              "- [x] I compiled the code successfully",
              "- [x] I manually verified",
              "- [x] I followed the project’s coding conventions",
              "- [x] I ensured no warnings or errors are introduced"
            ];
            
            let missing = requiredChecks.filter(item => !body.includes(item));
            
            const aiChecks = [
              "- [x] No AI assistance was used.",
              "- [x] AI was used for **partial assistance**",
              "- [x] AI was used for **full generation**"
            ];
            const hasAiCheck = aiChecks.some(item => body.includes(item));
            
            if (!hasAiCheck) {
               missing.push("An AI Usage option must be selected (tick one of the AI checkboxes)");
            }

            const usedAi = body.includes("- [x] AI was used for **partial") || body.includes("- [x] AI was used for **full");
            if (usedAi && !body.includes("- [x] I reviewed and validated all AI-generated code.")) {
               missing.push("- [x] I reviewed and validated all AI-generated code. (Required since AI was used)");
            }

            if (missing.length > 0) {
              core.setFailed(`PR is missing the following checked items:\n\n${missing.join("\n")}`);
            }

